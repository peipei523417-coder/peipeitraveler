# 完整的 codemagic.yaml 內容，請直接覆蓋
workflows:
  ios-build:
    name: iOS Capacitor Build
    max_build_duration: 60
    instance_type: mac_mini_m2

    integrations:
      app_store_connect: codemagic-ios

    environment:
      xcode: 16.2
      node: 22
      groups:
        - app_store_credentials
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.peipeigo.travel

    scripts:
      - name: Install dependencies
        script: npm install

      - name: Build web assets
        script: npm run build

      - name: Sync Capacitor iOS
        script: npx cap sync ios

      - name: Install CocoaPods
        script: |
          cd ios/App
          pod install

      - name: Set version numbers
        script: |
          cd ios/App
          # 自動遞增版本號，避免上傳重複版本
          agvtool new-marketing-version 1.0.$BUILD_NUMBER
          agvtool new-version -all $BUILD_NUMBER

      - name: Set up code signing
        script: |
          # 1. 載入你在 Settings 裡上傳的 Pei_iOS_Cert
          keychain add-certificates
          
          # 2. 強制載入並解碼 peipeigo_appstore 准考證
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          echo "$CM_PROVISIONING_PROFILE" | base64 --decode > ~/Library/MobileDevice/Provisioning\ Profiles/peipeigo_appstore.mobileprovision

      - name: Build iOS archive
        script: |
          cd ios/App
          # 使用手動簽署 (Manual Signing) 模式
          xcodebuild -workspace App.xcworkspace \
            -scheme App \
            -sdk iphoneos \
            -configuration Release \
            -archivePath $CM_BUILD_DIR/build/App.xcarchive \
            archive \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            PROVISIONING_PROFILE_SPECIFIER="peipeigo_appstore"

      - name: Generate ExportOptions and export IPA
        script: |
          cat > $CM_BUILD_DIR/ExportOptions.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>app-store</string>
              <key>signingStyle</key>
              <string>manual</string>
              <key>provisioningProfiles</key>
              <dict>
                  <key>com.peipeigo.travel</key>
                  <string>peipeigo_appstore</string>
              </dict>
          </dict>
          </plist>
          EOF

          xcodebuild -exportArchive \
            -archivePath $CM_BUILD_DIR/build/App.xcarchive \
            -exportOptionsPlist $CM_BUILD_DIR/ExportOptions.plist \
            -exportPath $CM_BUILD_DIR/build/ipa

    artifacts:
      - build/ipa/*.ipa

    publishing:
      email:
        recipients:
          - peitravel.dev@gmail.com
        notify:
          success: true
          failure: true
